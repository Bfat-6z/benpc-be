spring.application.name=agm_api
spring.messages.basename=messages
spring.thymeleaf.cache=false
spring.messages.encoding=UTF-8

# PORT: dùng bi?n môi tr??ng PORT (Railway s? truy?n), local thì m?c ??nh 8080
server.port=${PORT:8080}

# ================== DATABASE (MySQL) ==================
# Dùng bi?n môi tr??ng, n?u không có thì fallback v? c?u hình local hi?n t?i
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/QLBanHang?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Tuilaben2007!!}

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jackson.serialization.indent_output=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# T?m t?t nén (dev)
server.compression.enabled=false
